<%= simple_form_for @room, remote: true do |f| %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :color, collection: @colors, selected: @default_color %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
